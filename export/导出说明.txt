有三个导出格式，分别是ipl，nut，xml
ipl是单机游戏的格式
nut是联机服务端的格式
xml是联机客户端的格式

可以将xml放到store/maps文件夹里，然后就可以加载到服务器上了。
nut文件可以在游戏中输入/dofile <文件名> 然后进行加载。
如果出现无法加载的问题，那么你肯定是用了不正确的ID。
比如你创建一个ID为479的模型，你会发现它并不存在但是任然可以被创建，那是因为服务端支持，而客户端不支持导致的，但是客户端无法加载。